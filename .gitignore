# --- Gradle / build --------------------------------------------------------
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
out/
bin/

# --- IDE -------------------------------------------------------------------
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project

# --- OS --------------------------------------------------------------------
Thumbs.db
Desktop.ini
.DS_Store

# --- TerraForge runtime data ----------------------------------------------
# Геоданные НИКОГДА не попадают в репозиторий (см. DATA_SOURCES.md).
# Пути привязаны к корню: без ведущего слэша `data/` и `cache/` совпадают и с
# пакетами исходников (core/data, core/cache) и молча выкидывают их из репозитория.
/run/
/data/
/cache/
/source-data/
/server/
*.db
*.db-shm
*.db-wal
*.tif
*.tiff
*.hgt
*.asc
*.geojson
*.shp
*.dbf
*.shx
*.prj
*.tfdem
*.tfmask

# Локальные подготовленные артефакты и исходные растры
/artifacts/
/dist/

# Локальный кэш инструментов
/.claude/
/.codex/
/.cursor/
/.opencode/
/.windsurf/
/.zed/
/.playwright-mcp/
/.worktrees/

# codebase-index cache (machine-local; do not commit)
.claude/cache/codebase-index/
